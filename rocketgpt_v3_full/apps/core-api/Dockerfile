FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY server_stub_fastapi.py ./
EXPOSE 8080
ENV PORT=8080
CMD ["uvicorn", "server_stub_fastapi:app", "--host", "0.0.0.0", "--port", "8080"]