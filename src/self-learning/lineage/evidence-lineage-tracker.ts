import type { EvidenceReference } from "../types/evidence-reference";

export class EvidenceLineageTracker {
  track(_refs: EvidenceReference[]): void {
    // TODO: connect accepted learning to canonical evidence lineage.
  }
}
