name: _selfimprove_ingest_ci

on:

permissions:
  contents: read

  workflow_dispatch:
    inputs:
      enable:
        description: 'Set true to allow execution'
        required: true
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      dry_run:
        description: 'Set true to run with no side effects (no Supabase write)'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      subsystem:
        description: "Subsystem name for ledger row"
        required: false
        default: "ci-selfimprove"
      severity:
        description: "Ingest severity (must match rgpt-ledger-ingest ValidateSet)"
        required: false
        type: choice
        default: "high"
        options:
          - low
          - medium
          - high
          - critical
      title_prefix:
        description: "Title prefix"
        required: false
        default: "CI"
      confidence:
        description: "Confidence 0..1"
        required: false
        default: "0.80"

  workflow_call:
    inputs:
      enable:
        required: false
        type: string
        default: 'false'
      dry_run:
        required: false
        type: string
        default: 'true'
      subsystem:
        required: false
        type: string
        default: "ci-selfimprove"
      severity:
        required: false
        type: string
        default: "error"
      title_prefix:
        required: false
        type: string
        default: "CI"
      confidence:
        required: false
        type: string
        default: "0.80"

jobs:
  ingest:
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.enable == 'true' || github.event_name == 'workflow_call' && inputs.enable == 'true' }}
    runs-on: ubuntu-latest
    env:
      RGPT_SUBSYSTEM_FALLBACK: ci-selfimprove
      RGPT_SEVERITY_FALLBACK: error
      RGPT_TITLE_PREFIX_FALLBACK: CI
      RGPT_CONFIDENCE_FALLBACK: "0.80"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ingest CI failure into Self-Improve Ledger (aligned RPC)
        shell: pwsh
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          $dry = ("${{ inputs.dry_run }}" ).Trim()
          if ([string]::IsNullOrWhiteSpace($dry)) { $dry = "true" }
          if ($dry -eq "true") {
            Write-Host "[DRY_RUN] Ledger ingest skipped (no Supabase write)." -ForegroundColor Yellow
            Write-Host "[DRY_RUN] Would ingest ledger row using provided inputs." -ForegroundColor Yellow
            exit 0
          }

          $subsystem = ("${{ inputs.subsystem }}".Trim()); if ([string]::IsNullOrWhiteSpace($subsystem)) { $subsystem = $env:RGPT_SUBSYSTEM_FALLBACK }
          $severity  = ("${{ inputs.severity }}".Trim());  if ([string]::IsNullOrWhiteSpace($severity))  { $severity  = $env:RGPT_SEVERITY_FALLBACK }

          # Map workflow severities (info|warning|error|critical) -> ingest ValidateSet (low|medium|high|critical)
          $severityMap = @{
            "info"     = "low"
            "warning"  = "medium"
            "error"    = "high"
            "critical" = "critical"
            "low"      = "low"
            "medium"   = "medium"
            "high"     = "high"
          }
          $sevKey = $severity.ToLowerInvariant()
          if ($severityMap.ContainsKey($sevKey)) { $severity = $severityMap[$sevKey] }
          $prefix    = ("${{ inputs.title_prefix }}".Trim()); if ([string]::IsNullOrWhiteSpace($prefix)) { $prefix = $env:RGPT_TITLE_PREFIX_FALLBACK }
          $confRaw = ("${{ inputs.confidence }}".Trim()); if ([string]::IsNullOrWhiteSpace($confRaw)) { $confRaw = $env:RGPT_CONFIDENCE_FALLBACK }; $conf = [double]$confRaw

          $runUrl = "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          $origin = "gh:${{ github.repository }}:${{ github.workflow }}:${{ github.run_id }}:${{ github.job }}"

          $title = "$prefix :: ${{ github.workflow }} :: job ${{ github.job }}"
          $desc  = "Workflow: ${{ github.workflow }}`nJob: ${{ github.job }}`nRepo: ${{ github.repository }}`nRef: ${{ github.ref_name }}`nSHA: ${{ github.sha }}`nRun URL: $runUrl`nOrigin: $origin"

          ./.github/tools/rgpt-ledger-ingest.ps1 `
            -SUPABASE_URL $env:SUPABASE_URL `
            -SUPABASE_SERVICE_ROLE_KEY $env:SUPABASE_SERVICE_ROLE_KEY `
            -Subsystem $subsystem `
            -Severity $severity `
            -Title $title `
            -Description $desc `
            -Confidence $conf `
            -EvidenceRef $runUrl `
            -RelatedCommit "${{ github.sha }}" `
            -RelatedPR "" `
            -OriginRef $origin




