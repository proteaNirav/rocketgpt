name: P3 SafeMode Gate

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: p3-safemode-gate-main
  cancel-in-progress: true

jobs:
  gate:
    name: SafeMode Gate (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Gate summary
        run: |
          echo "SafeMode gate placeholder workflow is valid and running."
          echo "Next step: re-introduce the original gate checks once YAML parse is unblocked."