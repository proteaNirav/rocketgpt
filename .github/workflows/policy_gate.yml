name: Policy Gate (v4 Core AI)

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]

jobs:
  policy_gate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: YAML & workflow validation (stub)
        run: |
          echo "TODO: Add actionlint + YAML validation."
          echo "Block merge on malformed workflows."
