name: text-guard (smoke)

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "Smoke OK"
          echo "sha="